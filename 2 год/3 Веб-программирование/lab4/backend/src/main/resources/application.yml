server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lab4
    username: lab4
    password: lab4
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

app:
  cors:
    allowed-origins: "http://localhost:5173"
